import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Brutalist Editorial — Interactive Infographic Template",
  description: "A brutalist editorial template with GSAP scroll animations, animated infographics, and kinetic typography. Built with Next.js, Tailwind CSS, and GSAP.",
  keywords: ["Brutalist", "Editorial", "Infographic", "GSAP", "Scroll Animation", "Next.js", "Tailwind CSS"],
  authors: [{ name: "Z.ai" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Brutalist Editorial — Interactive Infographic Template",
    description: "Scroll-driven brutalist editorial with animated data visualization and motion transitions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
